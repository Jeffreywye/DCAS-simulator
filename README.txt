To run the program call make [command]

Commands are:

all : defaults to compile

compile : compiles the code and outputs an executable "run.exe"

run : compiles the code AND runs the executable that was produced by the compilation.

clean : removes the executable run.exe





